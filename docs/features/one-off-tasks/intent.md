# One-off tasks

Capture a one-off task in seconds, see it on Today, and finish or drop it — the
first end-to-end slice of Tasks through schema, API and PWA. Recurrence, habits
and the highlight come later.

## Notes for the spec

- Check the split first: `web/` scaffolding, where `logicalDate()` lives, and
  `shared/events` are each groundwork the slice needs and may be features of their
  own.
- Decide whether "done" means running on the Pi or locally over Tailscale.
